Executed external authoring command:

```bash
python refine_morning_glory_canopy_distribution.py --input <botanical_refinement_dir> --output <canopy_output_dir>
```

The integration run replaced only `BalconyMorningGloryBotanicalPlanter_Plant_*` scene nodes at their existing transforms. No formal benchmark scene was mutated. Material preview used the repository `render_material_preview.py` path and submitted all LOD0 faces.
